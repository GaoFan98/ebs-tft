schema_version: 1

# Direction adapters use the shared PyTorch training protocol. NeuralForecast's
# standard TFT remains a continuous forecasting engine, not this multiclass task.
engine: pytorch_direction_adapters
